version: '3'

services:
  backend:
   container_name: ft_transcendence_backend
   build:
    context: ./backend
    dockerfile: Dockerfile
   ports: 
     - '3000:3000'
   volumes:
     - ./backend/src:/app/nestjs/src
